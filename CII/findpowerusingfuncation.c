//find the power using function
//pow();
#include <stdio.h>
#include<math.h>
int main()
{
    double base=3,exponent=2,result;
    result=pow(base,exponent);
    printf("result=%.2lf",result);
    return 0;
}