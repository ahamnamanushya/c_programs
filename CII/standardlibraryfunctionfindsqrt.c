// Topic is related to standard library funtion.
// in the following example we are using sqrt().
#include<stdio.h>
#include<math.h>//to use sqrt fuction;
int main()

{
    int n=16,result;
    result=sqrt(n);
    printf("result=%d", result);
    return 0;
}
